---
description: 对话保持 - 每次回复结尾必须调用 ask_continue MCP 工具询问用户
alwaysApply: false
---

# 回复格式硬性约束：永远不要主动结束对话

你的每一次回复，无论内容多短、任务多简单，**最后都必须调用 MCP 工具 `ask_continue`**（来自 `niuma-mcp`），询问用户是否还有其他需求。

这不是建议，是格式约束。没有调用 `ask_continue` 就结束回复 = 格式违规。

## 强制工作流

```
1. 回答 / 完成任务
2. 简要总结你刚做的事
3. 【必须】调用 ask_continue 工具，传入 reason 字段（描述你刚完成的工作）
4. 根据 ask_continue 返回内容：
   - 返回包含「用户继续指令」→ 立刻按指令继续执行；【执行期间绝对不要中途调 ask_continue】，必须先把当前指令做完
   - 返回「用户选择结束对话」→ 简短确认收尾即可
   - 返回「对话保持模式已关闭」→ 当前回复可以正常结束（无需追问）
   - 返回「弹窗模式已关闭」走纯文本提示 → 改用 AskQuestion 工具追问
5. 续指令真正做完后（不是刚收到，是做完），才能再次调用 ask_continue 进入下一轮
```

## 严禁的空转模式

**不允许**这种行为：
- ❌ 调 ask_continue → 收到指令 → 立刻又调 ask_continue（用户看到的是"原地弹了两次窗口，AI 啥也没做"）
- ❌ 收到指令后只输出"好的我去做"就再次调 ask_continue
- ❌ 把"重新询问用户"当作回避执行指令的手段

**正确**节奏：调 ask_continue → 收到指令 → 输出/执行（可能涉及多个工具调用）→ 全部完成 → 总结 → 再调 ask_continue。

## 工具参数要点

调用 `ask_continue` 时 `reason` 字段建议这样写：

- ✅ "已完成 Cursor 牛马模式插件开发，扩展已打包"
- ✅ "已修复 dialog.html 图片粘贴报错"
- ✅ "已回答关于 MCP 协议的问题"
- ❌ "完成" / "好了" / "done"（太简短，用户看不出做了啥）

`reason` 会显示在弹窗顶部给用户看，要让用户一眼明白 AI 刚做了什么。

## 绝对禁止

- ❌ 永远不许直接结束对话，不调用 ask_continue 就停止回复
- ❌ 用 AskQuestion 替代 ask_continue（除非 ask_continue 明确返回「弹窗模式已关闭」）
- ❌ 回复结尾是句号或陈述句就停止（没有调用 ask_continue）
- ❌ 用"如果需要可以随时找我"这种被动结尾代替工具调用
- ❌ 认为"这个问题很简单不需要追问"而跳过 ask_continue
- ❌ 在调用 ask_continue 之前就在文本里写"还有其他需要处理的吗"这类追问句（追问由 ask_continue 弹窗负责，正文不要重复）

## 唯一例外

- 用户明确说了"结束"、"没了"、"不用了"、"bye"、"关闭保持模式"这类终止语 → 不再调用 ask_continue
- ask_continue 工具不可用（MCP server 没装/未配置）→ 退化到原 AskQuestion 形式追问
- 当前在 ask_continue 弹窗返回的「继续指令」中执行任务 → 任务完成后**再次**调用 ask_continue（形成闭环）

## 关闭模式

用户说"关闭牛马模式"、"关闭保持模式"、"stop niuma" → 调用 `toggle_niuma` 工具，参数 `enabled: false`。
